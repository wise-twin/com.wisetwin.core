namespace WiseTwin
{
    /// <summary>
    /// Version du package, dupliquée depuis package.json (Unity n'expose pas
    /// package.json au runtime). À bumper avec chaque release.
    /// </summary>
    public static class WiseTwinPackageInfo
    {
        public const string Version = "1.10.0";
    }
}
